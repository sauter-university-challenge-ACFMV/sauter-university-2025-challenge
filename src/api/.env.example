# FastAPI ingestion service configuration
ONS_API_URL=https://dados.ons.org.br/api/3/action/package_show
GCS_BUCKET_NAME=sauter-university-challenger-dev-raw
GOOGLE_CLOUD_PROJECT=sauter-university-challenger

# Authentication options (configure only one of the blocks below)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GOOGLE_CREDENTIALS_JSON={"type": "service_account", "project_id": "..."}

# Optional overrides
# LOG_LEVEL=INFO
# REQUEST_TIMEOUT_SECONDS=60
